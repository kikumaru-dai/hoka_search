<% flash.each do |key, value| %>
  <div class="alert alert-<%= 'success' if key == 'notice' %><%= 'danger' if key == 'alert' %> vw-100 text-center" role="alert" style="top: 56px;">
    <p><%= value %></p>
  </div>
<% end %>